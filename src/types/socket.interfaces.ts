export const enum SocketMethods {
  PUSH_UNDO = "PUSH_UNDO",
  UNDO = "UNDO",
  REDO = "REDO",
  CONNECTION = "CONNECTION",
  DRAW = "DRAW",
  SELECT = "SELECT",
  RESIZE = "RESIZE",
  CLEAR = "CLEAR",
}
